# AuroraFox final UI asset pack

The current V1.1.1.1 bugfix intentionally disables the previously corrupted shared JPG button/avatar textures.

Until `AuroraFox_UI_COMPLETE.zip` is supplied, the desktop UI uses safe flat neon states and the existing fallback fox SVG so no stretched/striped artifacts appear.

When the final archive is supplied, import each PNG by exact filename and exact native size. Do not stretch a shared atlas and do not reuse one image as multiple button states.
